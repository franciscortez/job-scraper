{
  "name": "onlinejobs-sheets-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run build",
    "push": "npm run check && clasp push",
    "open": "clasp open-script",
    "clasp": "clasp"
  },
  "dependencies": { "htmlparser2": "12.0.0" },
  "devDependencies": { "@google/clasp": "3.4.1", "esbuild": "0.28.2" }
}
